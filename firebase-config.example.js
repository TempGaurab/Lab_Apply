// Template. Copy this file to firebase-config.js (which is gitignored)
// and fill in your own project's values — or just run the app once
// through the GitHub Actions deploy, which generates firebase-config.js
// from repository secrets automatically. See README.md.
window.FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID",
  measurementId: "YOUR_MEASUREMENT_ID"
};
